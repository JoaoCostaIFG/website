---
- name: Config kernel params for sata controller pass-through
  ansible.builtin.copy:
    src: cmdline
    dest: /etc/kernel/cmdline
    owner: root
    group: root
    mode: '0644'
- name: Update systemd-boot params
  ansible.builtin.command:
    cmd: /usr/sbin/proxmox-boot-tool refresh
