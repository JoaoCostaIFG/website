version: "3.9"

services:
  tor:
    build: ./tor
    container_name: tor
    networks:
      - tor_internal
    volumes:
      - tor_socket:/socket
    restart: unless-stopped
  php:
    build:
      context: .
      dockerfile: ./php/Dockerfile
    container_name: php
    networks:
      - php_internal
    restart: unless-stopped
  nginx:
    build:
      context: .
      dockerfile: ./nginx/Dockerfile
    container_name: nginx
    ports:
      - 80:80
      - 443:443
    networks:
      - php_internal
      - tor_internal
    volumes:
      - tor_socket:/socket
    secrets:
      - joaocosta.dev
      - joaocosta.dev_ecc
    restart: unless-stopped

networks:
  php_internal:
    driver: bridge
  tor_internal:
    driver: bridge

volumes:
  tor_socket:
