services:
  php:
    volumes:
      - ./src:/app/main
      - ./wiki:/app/wiki
      - ./log:/var/log
  nginx:
    volumes:
      - ./src:/app/main
      - ./wiki:/app/wiki
      - ./log:/var/log

secrets:
  joaocosta.dev:
    file: ./nginx/keys
  joaocosta.dev_ecc:
    file: ./nginx/keys
